{
  "name": "shortcut-game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "bun sync",
    "sync": "lefthook install",
    "dev": "vite",
    "build": "vite build",
    "check": "biome check . && tsc",
    "fix": "biome check . --fix --unsafe",
    "fix:safe": "biome check . --fix"
  },
  "dependencies": {
    "pixi.js": "^8.8.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "lefthook": "^1.11.10",
    "typescript": "~5.7.3",
    "vite": "^6.2.0"
  }
}
